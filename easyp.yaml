lint:
  use:
    - DEFAULT
  service_suffix: API
generate:
  inputs:
    - directory: api
  plugins:
    - name: go
      out: .
      opts:
        module: github.com/graphene-ci/agent
    - name: go-grpc
      out: .
      opts:
        module: github.com/graphene-ci/agent
